/* config.js */

window.CKEDITOR.editorConfig = function ckConfig(config) {
    // Define changes to default configuration here.
    // For complete reference see:
    // http://docs.ckeditor.com/#!/api/CKEDITOR.config
};
